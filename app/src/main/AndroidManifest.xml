<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WiFiConfigurator">

        <receiver
            android:name=".WifiSuggestionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.wi_ficonfigurator.action.SUGGEST_WIFI" />
                <action android:name="com.example.wi_ficonfigurator.action.CLEAR_WIFI" />
                <action android:name="com.example.wi_ficonfigurator.action.LIST_WIFI" />
            </intent-filter>
        </receiver>
    </application>

</manifest>