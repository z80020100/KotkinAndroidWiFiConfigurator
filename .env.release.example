# Copy this file to .env.release (gitignored) and fill in your values.

# Default keystore path convention: ~/.keystores/<scope>/android-<purpose>.p12
RELEASE_STORE_FILE=$HOME/.keystores/$USER/android-release.p12

# Alias inside the keystore.
RELEASE_KEY_ALIAS=
